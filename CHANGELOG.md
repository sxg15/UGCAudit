# 更新日志

## 2026-05-30

- 模块定义新增启动方式说明，支持系统内置、HTTP、本地 EXE 传参等入口描述。
- 模块运行输入改为只包含待审核资源根目录和用户参数，不再接收上游模块结果。
- 模块运行结果改为只返回本模块的 Markdown 结果文本段，不再暴露可传递的输出数据。
- 三个预置审核模块改为文件夹模块定义，并补充各自的启动方式。
- 文本合规检测模块移除旧的上游 OCR 输出引用，改为在资源根目录中按文本文件匹配规则读取内容。
- 模块管理界面新增启动方式展示，方便确认模块接入方式。
